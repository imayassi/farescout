# Copy to .streamlit/secrets.toml locally, or paste these into the
# Streamlit Community Cloud "Secrets" settings when deploying.
# NEVER commit the real secrets.toml.

SEARCHAPI_API_KEY = "your-searchapi.io-key"
# SERPAPI_API_KEY = "your-serpapi.com-key"   # only if using the serpapi provider
OPENAI_API_KEY = "sk-..."
